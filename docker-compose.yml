services:
  frontend:
    build: .
    container_name: frontend-despachos
    restart: always
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=${VITE_API_URL}
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
